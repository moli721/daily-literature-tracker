请执行每日论文增量追踪并输出早报。

用户请求：
{description}

执行要求：
1. 先调用 arxiv_raw_check。
2. 将用户请求中出现的参数原样传给工具。
2.1 若用户未给参数，显式补齐默认值：
   category=cs.AI, keyword=agent, keyword_mode=token_set, token_set_scope=abstract,
   days=1, scan_limit=100, max_results=8, include_seen=false, update_tracker=true。
3. 不要自行添加任何 llm_api_key/llm_base_url/llm_model。
4. 直接输出最终日报，不要输出“任务已启动”。
