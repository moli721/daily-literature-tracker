请执行每日论文增量追踪并输出早报：

```text
{description}
```

执行要求：
1. 必须先调用 `arxiv_raw_check`。
2. 用户描述里显式出现的参数，必须原样透传给工具调用。
3. 若用户未给 `profile`，可留空；若给了 `profile`，必须透传以隔离追踪状态。
4. 默认只看最近 1 天，除非用户显式指定 `days`。
5. 输出遵循“今日论文早报”模板。
6. 无新增时明确写“今日无新增”；抓取失败时明确写“抓取失败”并附错误信息。
